<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nwzb.meeting_backend.mapper.BizNoteMapper">

    <select id="searchGlobal" resultType="com.nwzb.meeting_backend.model.vo.GlobalSearchVO">
        SELECT
            CAST(id AS CHAR) AS id,
            'NOTE' AS type,
            title AS title,
            SUBSTRING(content, 1, 50) AS highlightContent, create_time AS createTime
        FROM biz_note
        WHERE user_id = #{userId}
          AND (title LIKE #{keyword} OR content LIKE #{keyword})
    </select>

</mapper>